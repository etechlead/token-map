using Clever.TokenMap.Core.Enums;

namespace Clever.TokenMap.App.ViewModels;

public sealed record AnalysisMetricOption(AnalysisMetric Value, string Label);

public sealed record TokenProfileOption(TokenProfile Value, string Label);
